version: '3.8'

services:
  redis:
    image: redis:7
    ports:
      - 6379:6379
    stop_grace_period: 60s
    restart: always


networks:
  default:
    name: redis-streams
    driver: bridge